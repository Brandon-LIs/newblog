# GITALK 环境变量示例

本地开发 / Vercel 部署时配置（Gitalk 使用 GitHub OAuth App 认证）。
注意：Docusaurus 不会自动读取 .env 文件，本地开发请用 `export` 或直接写入 shell 环境，
部署时在 Vercel 的 Environment Variables 中设置。

```bash
# GitHub OAuth App: https://github.com/settings/developers
GITALK_CLIENT_ID=你的ClientID
GITALK_CLIENT_SECRET=你的ClientSecret
# 评论存放仓库（必须公开，owner 需有写权限）
GITALK_REPO=newblog
GITALK_OWNER=Brandon-LIs
```

## GitHub OAuth App 配置要点

- 创建位置: https://github.com/settings/developers → OAuth Apps → New OAuth App
- **Authorization callback URL**: `https://blog.oopss.top/`
  （GitHub 要求回调 URL 的路径必须包含重定向路径，Gitalk 会跳回当前文章页，
  因此填域名根路径即可覆盖所有页面）
- 本地调试时可用第二个测试 App，callback 填 `http://localhost:3000/`
- `clientSecret` 会内嵌到前端构建产物中，这是 Gitalk 的设计限制（建议公开仓库）

## 评论仓库要求

- 仓库必须公开
- 开启 Issues 功能
- `GITALK_OWNER`（或 admin 数组中的账号）需有仓库写权限
